// Example Configuration
// In this project, we store the API Key in localStorage for simplicity (Client-Side).
// You do not need to edit this file to make the app work.
// Just open the app and enter the key when prompted.

const CONFIG = {
    appName: "Via Boat AI",
    version: "1.0.0"
};
